# Athena OS 环境变量配置示例
# 复制此文件为 .env 并填入实际值

# ======== 基础配置 ========
# 运行环境: production / development
ENV=production
# 日志级别: DEBUG/INFO/WARNING/ERROR
LOG_LEVEL=INFO

# ======== 交易所 API 配置 (OKX) ========
OKX_API_KEY=your_api_key_here
OKX_SECRET_KEY=your_secret_key_here
OKX_PASSPHRASE=your_passphrase_here
# 是否使用模拟盘: true/false
USE_DEMO=true

# ======== 核心资金配置 ========
# 账户总本金 (USDT) - 用于风控计算
TOTAL_CAPITAL=10000.0

# ======== 全局风控配置 ========
# 单笔风险比例 - 每笔交易风险不超过总资金的百分比
# 0.005 = 0.5% (保守/新手)
# 0.01 = 1.0% (默认/推荐)
# 0.02 = 2.0% (激进/专业)
# 如不设置，默认使用 1.0%
# RISK_PER_TRADE_PCT=0.01

# 最大单笔订单金额 (USDT)
# 如不设置，自动计算为 总资金 × 5.0 (支持 5x 杠杆)
# 例如：10000 U 本金 → 50000 U 单笔限额
# 如需手动控制，可设置此变量
# MAX_ORDER_AMOUNT=50000.0

# ======== 策略选择 ========
# 启用的策略名称: scalper_v1
ACTIVE_STRATEGY=scalper_v1

# ======== ScalperV1 策略参数 ========
# 是否启用 ScalperV1 策略: true/false
ENABLE_SCALPER_V1=true
# ScalperV1 策略资金 (USDT)
SCALPER_CAPITAL=100.0
# 交易对 (建议选择流动性好且波动大的，如 SOL-USDT-SWAP)
SCALPER_SYMBOL=SOL-USDT-SWAP
# 最小流速阈值 (USDT/秒) - 只有超过此成交量才监测
SCALPER_MIN_FLOW=1000.0
# 买卖失衡阈值 (倍数) - 买比卖多 N 倍才开仓
SCALPER_IMBALANCE_RATIO=3.0
# 杠杆倍数 (RiskProfile)
SCALPER_LEVERAGE=5.0
# 止盈百分比 (0.002 = 0.2%)
SCALPER_TAKE_PROFIT_PCT=0.002
# 硬止损百分比 (0.01 = 1%)
SCALPER_STOP_LOSS_PCT=0.01
# 时间止损 (秒) - 持仓超过此时间强制平仓
SCALPER_TIME_LIMIT_SECONDS=5
# 单笔最大订单金额 (USDT)
SCALPER_MAX_ORDER_SIZE=500.0
# 单笔最大亏损占比 (0.02 = 2%)
SCALPER_SINGLE_LOSS_CAP=0.02
# 每日最大亏损占比 (0.05 = 5%)
SCALPER_DAILY_LOSS_LIMIT=0.05
# 持仓大小 (None = 基于风险自动计算)
# SCALPER_POSITION_SIZE=0.1
# Maker 挂单超时时间 (秒) - 挂单未成交超时自动撤单
# SCALPER_MAKER_TIMEOUT=2.0

# 冷却时间 (秒) - 平仓后等待时间
# HFT 策略应使用极短冷却（默认 0.1 秒），以支持快速追单
# 保守策略可设置为 1.0-5.0 秒
# SCALPER_COOLDOWN_SECONDS=0.1

# ======== ScalperV1 插队和追单配置 (HFT 高级功能) ========
# 最小价格跳动单位 (USDT) - 插队时加价的幅度
# 默认 0.01 USDT，根据交易对调整
# SCALPER_TICK_SIZE=0.01

# 最大追单距离 (百分比) - 防止无限追高的风控保护
# 默认 0.001 = 0.1%，超过此距离则放弃追单
# SCALPER_MAX_CHASE_DISTANCE=0.001

# 是否启用追单机制 - 当被挤到队列后面时自动追价
# 默认启用，设为 false 可关闭追单
# SCALPER_ENABLE_CHASING=true
