# ============================================================================
# Athena Trader 生产环境依赖
# Production Dependencies for Athena Trader
# ============================================================================
#
# 使用方法：
#   pip install -r requirements-prod.txt
#
# 本文件仅包含运行时必需的依赖，不包含开发工具。
# 适用于生产环境和轻量级部署（1G 内存）。
# ============================================================================

# HTTP 和异步通信
aiohttp==3.10.5
websockets==13.1
requests==2.32.3

# 数据验证和配置
pydantic==2.8.2
pydantic-settings==2.4.0
python-dotenv==1.0.0

# 安全加密
cryptography==42.0.8

# 交易所 SDK（备用）
ccxt==4.2.25

# 数据库（生产环境需要）
redis==5.0.1
psycopg2-binary==2.9.9
asyncpg==0.29.0

# Web 框架（用于监控 API）
fastapi==0.111.1
uvicorn==0.30.1

# 监控工具
watchdog==5.0.2

# ============================================================================
# 优化说明
# ============================================================================
#
# 2026年1月14日 - 生产环境依赖优化
# - 已移除 pandas（节省 200-300MB 内存）
# - 不包含测试框架（pytest）
# - 不包含覆盖率工具（pytest-cov）
#
# 内存占用估算：
# - 核心依赖：~50-80MB
# - 数据库驱动：~30-50MB
# - 总计：~80-130MB（远低于 1G 内存限制）
#
# 如果需要扩展依赖：
#   1. 评估内存占用
#   2. 确认是否为运行时必需
#   3. 优先使用轻量级替代方案
# ============================================================================
